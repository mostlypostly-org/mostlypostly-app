---
phase: ad-hoc-debug
task: 5
total_tasks: 6
status: paused
last_updated: 2026-03-19T16:59:06.083Z
---

<current_state>
Quick task 260319-fii (aiGenerateDesc fix) is COMPLETE and committed.

However, continued testing revealed the `aiGen` button on the edit campaign page is ALSO broken. A second fix was applied (commit 78d6aac) making aiGen async with null checks. Deployed to main + dev.

User ran `typeof aiGen` in browser console but paused before sharing the result. That result is the critical next diagnostic step.

There is also a NEW feature request pending (not started): affiliate URL injection in social posts.
</current_state>

<completed_work>

- quick-260319-fii COMPLETE: aiGenerateDesc + aiGenerateDescBrand fixed (inline getElementById → pass IDs as strings)
- aiGen on edit campaign page fixed: rewritten as async/await with null guards, confirm dialog for existing content, try/catch (commit 78d6aac)
- All fixes pushed to main (prod) and dev (staging)

</completed_work>

<remaining_work>

- Confirm aiGen fix actually works — get typeof aiGen result from user
  - If "undefined": script block still failing to parse, investigate CSRF middleware or template rendering
  - If "function": likely the NEW confirm() dialog is the issue (description is pre-filled on edit page → confirm appears → user might not notice it)
- NEW feature: Affiliate URL injection in social posts
  - FB posts: replace booking URL with affiliate URL (using UTM tracking token injection via buildTrackingToken)
  - IG posts: change "Book now, link in bio" → "Shop now, link in bio" when affiliate URL is set

</remaining_work>

<decisions_made>

- aiGen fix approach: match aiGenerateDesc pattern exactly (async/await, getElementById null checks, confirm dialog for pre-filled content, catch → alert)
- Previous session incorrectly noted aiGen was "already fixed in 077230b" — it was not; same silent-failure pattern existed
- Response size for edit campaign page (3432 bytes) seems small but is plausible with gzip compression; PIN page without session is 1992 bytes

</decisions_made>

<blockers>

- typeof aiGen result not yet confirmed — determines if further debugging needed
- Affiliate URL feature not started — needs /gsd:quick --discuss to scope properly

</blockers>

<context>
All three Platform Console AI Generate button locations now have fixes deployed:
1. Inline per-vendor form: aiGenerateDesc (fixed in 260319-fii)
2. Top-level add campaign form: aiGenerateDesc (fixed in 260319-fii)
3. Brand detail page: aiGenerateDescBrand (fixed in 260319-fii)
4. Edit campaign page: aiGen (fixed in 78d6aac — this session)

The edit campaign page delivers 3432 bytes. The key unknown: is aiGen defined in the browser or not? If undefined → script parse error still happening. If function → the confirm() dialog we added (for pre-filled description) may be confusing the user.

Affiliate URL feature: salon_vendor_feeds.affiliate_url is already stored. In composeFinalCaption.js and/or scheduler.js, when a vendor post has an affiliate URL, the FB caption's "Book now: [url]" becomes a UTM-tracked affiliate link, and the IG caption's "Book now, link in bio" becomes "Shop now, link in bio".
</context>

<next_action>
Start by asking: "What did `typeof aiGen` return in the console?" Then handle based on result. After resolving, start affiliate URL feature with `/gsd:quick --discuss`.
</next_action>
