---
phase: 02-content-engine
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
requirements: [RECYC-01, RECYC-02, RECYC-03, RECYC-04, RECYC-05, RECYC-06, RECYC-07, RECYC-08, RECYC-09, RECYC-10, RECYC-11, SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, SCHED-06]
must_haves:
  truths:
    - "All vitest tests pass"
    - "Server starts without crash"
    - "Admin toggles render and save correctly"
    - "Database view shows Recycle, Block, and Undo actions"
    - "Dashboard notice banner appears when recycled posts exist"
  artifacts: []
  key_links: []
---

<objective>
Run full test suite and verify all Content Engine features work end-to-end.

Purpose: Final gate before marking Phase 2 complete — ensures recycler, cadence scheduler, admin UI, and dashboard actions all function correctly together.
Output: Verification of all 17 requirements (RECYC-01 through RECYC-11, SCHED-01 through SCHED-06).
</objective>

<execution_context>
@/Users/troyhardister/.claude/get-shit-done/workflows/execute-plan.md
@/Users/troyhardister/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-engine/02-CONTEXT.md
@.planning/phases/02-content-engine/02-01-SUMMARY.md
@.planning/phases/02-content-engine/02-02-SUMMARY.md
@.planning/phases/02-content-engine/02-03-SUMMARY.md
@.planning/phases/02-content-engine/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify server starts</name>
  <files>none — verification task only</files>
  <read_first>
    - vitest.config.mjs (test configuration)
    - src/core/contentRecycler.test.js (recycler tests)
    - src/core/pickNextPost.test.js (scheduler tests)
  </read_first>
  <action>
    Run the full vitest test suite:
    ```bash
    npx vitest run --reporter=verbose
    ```

    Then verify the server can start without crashing (syntax + import check):
    ```bash
    node -e "
      import('./src/scheduler.js').then(m => {
        console.log('scheduler.js loaded OK');
        console.log('DEFAULT_PRIORITY:', m.DEFAULT_PRIORITY);
        process.exit(0);
      }).catch(err => {
        console.error('FAILED:', err.message);
        process.exit(1);
      });
    "
    ```

    If any test fails, fix the failing test or implementation. If server fails to start, diagnose and fix the import chain.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all tests passing
    - No test files show failures
    - Server module loads without import errors
  </acceptance_criteria>
  <done>All vitest tests pass. Server imports resolve correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Content Engine features</name>
  <files>none — verification task only</files>
  <action>
    Present the verification checklist to the user. Wait for them to test each feature in the running dev server and report results.
  </action>
  <verify>User confirms all features working or reports issues to fix</verify>
  <done>User types "approved" confirming all Content Engine features work correctly</done>
  <what-built>
    Complete Content Engine — auto-recycler (fires when queue depth is less than 3 AND 48hr since last publish), intelligent cadence scheduler (pickNextPost with 7-day rolling distribution), Admin toggles (auto-recycle + caption refresh), Database view actions (Recycle button, Block toggle, Undo recycled posts), and dashboard auto-recycle notice banner.
  </what-built>
  <how-to-verify>
    1. Start the dev server — `npm start` or your usual startup command
    2. Admin toggles — Navigate to Admin then Manager Rules. Verify:
       - "Auto-Recycle" toggle appears (Enabled/Disabled dropdown)
       - If salon is on Growth/Pro plan: "Caption Refresh on Recycle" toggle also appears
       - If salon is on Starter/trial: Caption Refresh toggle is NOT visible
       - Toggle Auto-Recycle to "Enabled", save, reload — confirms it persisted
    3. Database view — Navigate to Database (sidebar link). Verify:
       - Published post rows show "Recycle" button (blue accent) and "Block" button (muted)
       - Click "Block" on a post — button changes to "Blocked" (red)
       - Click "Blocked" again — reverts to "Block" (muted)
       - Click "Recycle" on a published post — success notice appears, new post created
       - New recycled post shows a "Recycled" badge in the post type column
       - Recycled post shows an "Undo" button
       - Click "Undo" — confirmation dialog appears, clicking OK deletes the recycled copy
    4. Dashboard notice — Navigate to Manager Dashboard. If any posts were recycled this week:
       - Blue notice banner appears at top: "X post(s) were auto-recycled this week."
       - "View in Database" link works
       - Close button (x) removes the banner
    5. Non-published rows — Verify that draft, pending, and failed posts do NOT show Recycle/Block buttons (should show em-dash)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full vitest suite green
- Human verification of all UI components
- All 17 phase requirements visually confirmed working
</verification>

<success_criteria>
All tests pass, server runs, and human verification confirms all Content Engine features work correctly. Phase 2 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-engine/02-05-SUMMARY.md`
</output>
