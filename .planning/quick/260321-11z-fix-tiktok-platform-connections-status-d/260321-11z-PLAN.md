---
phase: quick
plan: 260321-11z
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/schedulerConfig.js
autonomous: true
requirements: [QUICK]
must_haves:
  truths:
    - "TikTok row in Platform Connections shows green Connected pill or gray Not connected pill matching FB and IG pattern"
    - "TikTok daily max input appears in the Platform Daily Caps card alongside FB and IG caps, not in the Platform Connections card"
  artifacts:
    - path: "src/routes/schedulerConfig.js"
      provides: "Updated Scheduler page with TikTok status badge and relocated daily cap"
  key_links:
    - from: "salon.tiktok_account_id"
      to: "TikTok status pill"
      via: "conditional class in Platform Connections card"
      pattern: "tiktok_account_id.*Connected"
---

<objective>
Fix two UI issues on the Scheduler page's Platform Connections and Platform Daily Caps cards:

1. Add a connected/not-connected status badge to the TikTok row in Platform Connections (matching the existing FB and IG badge pattern).
2. Move the TikTok "Max posts per day" input from the Platform Connections card into the Platform Daily Caps card (where FB and IG caps already live).

Purpose: Consistent UI across all platform rows on the Scheduler page.
Output: Updated `src/routes/schedulerConfig.js` with both fixes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/schedulerConfig.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TikTok status badge and move daily cap input</name>
  <files>src/routes/schedulerConfig.js</files>
  <action>
Two changes in the GET `/manager/scheduler` route handler:

**Change 1 — TikTok status badge in Platform Connections card (lines ~379-392):**

Replace the TikTok row content. Currently the right side shows only an `<input type="number">` for daily max. Change it to match the FB/IG pattern:
- Remove the `<input>` from this row entirely (it moves to Platform Daily Caps).
- Change the subtitle from "Max posts per day" to show the TikTok username if connected (`salon.tiktok_username ? "@" + salon.tiktok_username : "Not connected — set up in Integrations"`).
- Add a status pill on the right side matching FB (line 358) and IG (line 374) exactly:
```html
<span class="inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-[10px] font-semibold ${salon.tiktok_account_id && salon.tiktok_refresh_token ? "bg-green-50 text-green-700 border border-green-200" : "bg-gray-100 text-gray-400 border border-gray-200"}">
  ${salon.tiktok_account_id && salon.tiktok_refresh_token ? "Connected" : "Not connected"}
</span>
```

**Change 2 — Move TikTok daily cap into Platform Daily Caps card (after line ~463):**

Inside the Platform Daily Caps card, add a third column to the `grid gap-6 sm:grid-cols-2` — change it to `sm:grid-cols-3`. Add a TikTok cap input matching the FB and IG pattern:
```html
<div>
  <label class="block text-xs font-semibold text-mpMuted mb-1.5">TikTok Feed (posts/day)</label>
  <input type="number" id="tkCapInput" name="tiktok_daily_max"
    value="${tkCap}" min="1" max="${planDef.max_daily}"
    oninput="updatePace()"
    class="w-full rounded-lg border border-mpBorder px-3 py-2 text-sm text-mpCharcoal focus:border-mpAccent focus:outline-none" />
  <div class="mt-1.5 flex items-center justify-between text-[11px]">
    <span class="text-mpMuted">Plan max: ${planDef.max_daily}/day</span>
    <span class="text-mpMuted">Rec: 1–3</span>
  </div>
</div>
```

Also update the section heading comment from `FB + IG only` to `FB + IG + TikTok`.

Verify the `updatePace()` JS function (if it exists in the page script) references the new `tkCapInput` — if it only sums FB + IG, add `tkCapInput` to the sum. If `updatePace` does not exist or is trivial, skip this.
  </action>
  <verify>
    <automated>node -e "import('./src/routes/schedulerConfig.js').then(() => console.log('Module loads OK')).catch(e => { console.error(e.message); process.exit(1) })"</automated>
  </verify>
  <done>
    - TikTok row in Platform Connections shows a green "Connected" or gray "Not connected" pill identical to FB and IG rows
    - TikTok row no longer has an inline number input
    - TikTok daily cap input appears in the Platform Daily Caps card as a third column alongside FB and IG
    - TikTok subtitle shows @username when connected, guidance text when not
  </done>
</task>

</tasks>

<verification>
- Load `/manager/scheduler` in browser
- Confirm TikTok row in Platform Connections shows status pill matching FB/IG pattern
- Confirm TikTok daily cap input is in the Platform Daily Caps section
- Confirm form submission still saves all three cap values (FB, IG, TikTok)
</verification>

<success_criteria>
TikTok has consistent status display and its daily cap input lives alongside FB and IG caps in the dedicated section.
</success_criteria>

<output>
After completion, create `.planning/quick/260321-11z-fix-tiktok-platform-connections-status-d/260321-11z-SUMMARY.md`
</output>
