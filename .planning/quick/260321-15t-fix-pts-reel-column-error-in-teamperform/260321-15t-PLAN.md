---
phase: quick
plan: 260321-15t
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/053_pts_reel.js
  - migrations/index.js
  - src/routes/teamPerformance.js
autonomous: true
requirements: [BUG-pts-reel, UX-sort-points]
must_haves:
  truths:
    - "Saving point values on Performance page succeeds without SqliteError"
    - "Point categories display sorted by value descending after save and on initial load"
  artifacts:
    - path: "migrations/053_pts_reel.js"
      provides: "pts_reel column on gamification_settings"
      contains: "pts_reel"
    - path: "src/routes/teamPerformance.js"
      provides: "Fixed settings save + sorted point values display"
  key_links:
    - from: "src/core/gamification.js"
      to: "gamification_settings"
      via: "DEFAULT_POINTS includes reel key"
      pattern: "reel.*20"
---

<objective>
Fix the SqliteError caused by missing `pts_reel` column in `gamification_settings` table, and sort point value categories by value descending in the Performance settings UI.

Purpose: The Performance page crashes on save because `DEFAULT_POINTS` in gamification.js includes `reel: 20` but migration 025 never created a `pts_reel` column. Additionally, point categories should display highest-value first for better UX.

Output: Working save-settings route + sorted point values display.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/teamPerformance.js
@src/core/gamification.js
@migrations/025_gamification.js
@migrations/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pts_reel migration and register it</name>
  <files>migrations/053_pts_reel.js, migrations/index.js</files>
  <action>
Create `migrations/053_pts_reel.js` that adds the missing `pts_reel INTEGER` column to `gamification_settings` using the standard try/catch ALTER TABLE pattern (same as other column-add migrations in this project).

Then register migration 053 in `migrations/index.js` — import it and add to the migrations array following the existing pattern.
  </action>
  <verify>
    <automated>node -e "import('./migrations/053_pts_reel.js').then(m => console.log('OK:', typeof m.run === 'function'))"</automated>
  </verify>
  <done>Migration file exists, exports run function that ALTERs gamification_settings to add pts_reel INTEGER column. Migration registered in index.js.</done>
</task>

<task type="auto">
  <name>Task 2: Sort point value categories by value descending in UI</name>
  <files>src/routes/teamPerformance.js</files>
  <action>
In the GET `/manager/performance` handler (~line 205), the `ptRows` variable iterates `Object.entries(DEFAULT_POINTS)` in insertion order. Change this to sort entries by their current point value (from settings or default) descending before mapping to HTML rows.

Replace:
```js
const ptRows = Object.entries(DEFAULT_POINTS).map(([key, def]) => {
```

With logic that sorts entries by current value descending:
```js
const ptRows = Object.entries(DEFAULT_POINTS)
  .sort((a, b) => {
    const valA = settings?.[`pts_${a[0]}`] ?? a[1];
    const valB = settings?.[`pts_${b[0]}`] ?? b[1];
    return valB - valA;
  })
  .map(([key, def]) => {
```

This ensures both initial page load AND the redirect after save (`?saved=1`) show categories sorted highest-value first.
  </action>
  <verify>
    <automated>grep -A5 "Object.entries(DEFAULT_POINTS)" src/routes/teamPerformance.js | grep -q "sort"</automated>
  </verify>
  <done>Point value categories render sorted by current value descending on both initial load and after save redirect. Reel (20pts) appears first by default.</done>
</task>

</tasks>

<verification>
1. Start the app locally — migrations run on boot, `pts_reel` column is added
2. Navigate to `/manager/performance` — no crash
3. Change point values and click Save — no SqliteError, page reloads with values preserved
4. Point categories appear sorted highest value first
</verification>

<success_criteria>
- No `SqliteError: no such column: pts_reel` on save
- Point categories sorted by value descending in the settings card
- Existing point values preserved (migration is additive)
</success_criteria>

<output>
After completion, create `.planning/quick/260321-15t-fix-pts-reel-column-error-in-teamperform/260321-15t-SUMMARY.md`
</output>
