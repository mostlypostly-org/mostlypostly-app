---
phase: quick
plan: 260319-q5j
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/046_vendor_frequency_controls.js
  - migrations/index.js
  - src/core/vendorScheduler.js
  - src/core/vendorSync.js
  - src/routes/vendorAdmin.js
  - src/routes/vendorFeeds.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Vendor campaigns auto-get a 30-day expiry when expires_at is NULL at insert time"
    - "Scheduler enforces a minimum 3-day gap between vendor posts per salon (floor)"
    - "Platform Console can set min_gap_days and platform_max_cap per brand"
    - "Platform Console has a Renew button per campaign that extends expires_at by +30 days from current expiry"
    - "tone_direction is removed from CSV template, Platform Console forms, and the OpenAI prompt"
    - "New campaigns default to frequency_cap 3 (down from 4)"
  artifacts:
    - path: "migrations/046_vendor_frequency_controls.js"
      provides: "Adds min_gap_days and platform_max_cap to vendor_brands"
    - path: "src/core/vendorScheduler.js"
      provides: "Min-gap enforcement and updated default cap (3)"
    - path: "src/routes/vendorAdmin.js"
      provides: "Renew button, min_gap_days/platform_max_cap fields, tone_direction removed"
  key_links:
    - from: "vendorScheduler.js processCampaign()"
      to: "posts table"
      via: "SELECT last vendor post date for salon, compare to min_gap_days from vendor_brands"
      pattern: "scheduled_for.*vendor_campaign_id"
---

<objective>
Implement three-layer vendor posting frequency controls: platform floor (min gap between posts), platform ceiling (max monthly cap per brand), and auto-expiration. Also add campaign Renew button in Platform Console and remove the redundant tone_direction field throughout.

Purpose: Protect brand partners from overexposure while giving high-volume salons room to align with their brand cadence. Auto-expiry prevents stale campaigns from posting indefinitely.
Output: Migration, scheduler enforcement, Platform Console UI updates, tone_direction removal.
</objective>

<execution_context>
@/Users/troyhardister/.claude/get-shit-done/workflows/execute-plan.md
@/Users/troyhardister/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260319-q5j-audit-vendor-campaign-posting-frequency-/260319-q5j-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + vendorSync auto-expiry + scheduler min-gap enforcement</name>
  <files>
    migrations/046_vendor_frequency_controls.js
    migrations/index.js
    src/core/vendorSync.js
    src/core/vendorScheduler.js
  </files>
  <action>
**Migration 046** (`migrations/046_vendor_frequency_controls.js`):
Use the same PRAGMA + conditional ADD COLUMN pattern from migration 045. Add to `vendor_brands`:
- `min_gap_days INTEGER DEFAULT 3`
- `platform_max_cap INTEGER DEFAULT 6`

Register migration in `migrations/index.js` using the same import pattern as 045 — add `import * as m046 from './046_vendor_frequency_controls.js'` and push to the migrations array.

**vendorSync.js — auto-expiry on insert** (`src/core/vendorSync.js`):
Find the INSERT into `vendor_campaigns` (around line 620). The current insert passes `expires_at` from the parsed row directly. Add a fallback: if `expires_at` is falsy/null after parsing, set it to 30 days from today using `new Date(Date.now() + 30 * 24 * 60 * 60 * 1000).toISOString().slice(0, 10)` before the INSERT. The variable `frequency_cap: 4` on line ~658 should also change to `frequency_cap: 3`.

**vendorScheduler.js — min-gap enforcement + default cap update**:
1. In `processSalon()` (line ~174), augment the `enabledVendors` query to also JOIN `vendor_brands` and fetch `b.min_gap_days`, `b.platform_max_cap` per vendor. Return these alongside `vendor_name`.

2. In `processCampaign()` (line ~224), change the default cap fallback from `|| 4` to `|| 3` (line ~228).

3. Add a min-gap check BEFORE the monthly cap check. After fetching `cap`, query for the most recent post created from ANY campaign of this vendor for this salon:
```sql
SELECT MAX(scheduled_for) AS last_vendor_post
FROM posts
WHERE salon_id = ?
  AND vendor_campaign_id IN (
    SELECT id FROM vendor_campaigns WHERE vendor_name = ?
  )
  AND status IN ('manager_pending', 'manager_approved', 'published')
```
If `last_vendor_post` is set, compute days since last post. If `daysSince < min_gap_days`, log and return false. Pass `vendorName` and `minGapDays` into `processCampaign()` — update its signature to accept them. Update all call sites in `processSalon()`.

Note: `min_gap_days` comes from the `vendor_brands` JOIN added in step 1.
  </action>
  <verify>
    <automated>node -e "import('./migrations/046_vendor_frequency_controls.js').then(m => console.log('Migration exports run:', typeof m.run))"</automated>
  </verify>
  <done>Migration 046 exports a `run()` function and is registered in index.js. vendorSync.js defaults frequency_cap to 3 and auto-sets expires_at when null. vendorScheduler.js enforces min_gap_days before creating a vendor post.</done>
</task>

<task type="auto">
  <name>Task 2: Platform Console — min_gap/platform_max fields, Renew button, auto-expiry, tone_direction removal</name>
  <files>
    src/routes/vendorAdmin.js
    src/routes/vendorFeeds.js
  </files>
  <action>
**vendorAdmin.js changes:**

1. **CSV_HEADERS** (line ~58): Remove `"tone_direction"` from the array. Remove its value from `CSV_EXAMPLE` (line ~79). Update column count accordingly.

2. **Campaign create/edit forms** (lines ~475, ~1046, ~1954, ~2139 — there are four form instances): Remove the `tone_direction` input field from all four. Change all `value="4"` defaults for `frequency_cap` inputs to `value="3"`. Also update the `max="30"` to `max="${safe(brand.platform_max_cap || 6)}"` where the brand object is in scope; for forms that don't have the brand object readily available, use a hard-coded `max="6"` for now.

3. **CSV upload INSERT** (line ~1383–1402): Remove `tone_direction` from the INSERT column list and params. The `getCol(row, "frequency_cap")` default (line ~1402) — ensure fallback is `3` not `4`. Add auto-expiry: after parsing `expires_at = getCol(row, "expires_at") || null`, add: `if (!expires_at) expires_at = new Date(Date.now() + 30*24*60*60*1000).toISOString().slice(0,10);`

4. **Manual campaign create POST** (line ~1492): Remove `tone_direction` from body parse and INSERT. Change `|| 4` default for `frequency_cap` to `|| 3`. Add auto-expiry fallback for `expires_at` (same pattern as above).

5. **Manual campaign edit POST** (line ~1536): Remove `tone_direction` from UPDATE SET clause. Change `|| 4` to `|| 3`.

6. **Brand detail view — add min_gap_days and platform_max_cap fields**: Find where the brand edit form is rendered (search for `platform_max_cap` or the brand config section). Add two number inputs: `min_gap_days` (label: "Min gap between posts (days)", default 3, min 1, max 30) and `platform_max_cap` (label: "Max posts/month per campaign (ceiling)", default 6, min 1, max 30). In the corresponding POST handler that saves brand config, add `db.prepare("UPDATE vendor_brands SET min_gap_days = ?, platform_max_cap = ? WHERE vendor_name = ?").run(minGap, platformMax, name)`.

7. **Campaign Renew button in Platform Console**: In the campaign list table (around line ~1785 where cap is shown), add a Renew form button after the expiry cell:
```html
<form method="POST" action="/internal/vendors/campaign/renew${qs(req)}" style="display:inline">
  <input type="hidden" name="campaign_id" value="${safe(c.id)}" />
  <button type="submit" class="text-xs text-blue-600 underline">Renew +30d</button>
</form>
```
Add the POST route handler: `router.post("/campaign/renew", requireSecret, requirePin, (req, res) => { ... })`. It should: get `campaign_id` from body, fetch the campaign, compute new expiry as `new Date(new Date(campaign.expires_at || new Date()).getTime() + 30*24*60*60*1000).toISOString().slice(0,10)`, run UPDATE, redirect back with `?renewed=1`. The `?renewed=1` flash message is already handled at line ~308.

**vendorFeeds.js changes:**
The `|| 4` default for `cap` at line ~158 and ~526 — change both to `|| 3`. No other changes needed here; the salon-side frequency_cap UI is a future enhancement not in scope for this task.
  </action>
  <verify>
    <automated>node --input-type=module &lt;&lt;&lt; "import './src/routes/vendorAdmin.js'" 2&gt;&amp;1 | head -5</automated>
  </verify>
  <done>Platform Console no longer shows tone_direction in any form. All frequency_cap defaults show 3. Renew button appears per campaign and extends expires_at by +30 days. Brand config section shows min_gap_days and platform_max_cap editable fields. vendorFeeds.js cap defaults are 3.</done>
</task>

<task type="auto">
  <name>Task 3: Remove tone_direction from OpenAI prompt in vendorScheduler.js</name>
  <files>src/core/vendorScheduler.js</files>
  <action>
In `generateVendorCaption()` (line ~75), find the prompt template block that includes `tone_direction` (line ~101):

```js
${campaign.tone_direction ? `Brand tone direction: ${campaign.tone_direction}...` : ""}
```

Remove this line entirely from the prompt string. The salon's own `salons.tone` field already drives the voice — `tone_direction` is redundant and its removal is intentional.

Do not modify any other part of the function. Do not touch the `brandCaption` path or any other prompt lines.

After removing the line, verify the prompt template string still produces valid output by tracing the surrounding template literal — ensure no stray newlines or syntax errors are left behind.
  </action>
  <verify>
    <automated>node -e "import('./src/core/vendorScheduler.js').then(() => console.log('vendorScheduler loaded OK')).catch(e => { console.error(e.message); process.exit(1); })"</automated>
  </verify>
  <done>generateVendorCaption() no longer references tone_direction. Module loads without error.</done>
</task>

</tasks>

<verification>
1. Run the app locally and confirm migration 046 applies cleanly: `node -e "import('./migrations/index.js').then(m => m.runMigrations && m.runMigrations())"` (or the app's startup sequence)
2. In Platform Console, create a campaign without an expiry date — confirm the saved row has expires_at set to ~30 days out
3. In Platform Console brand config, confirm min_gap_days and platform_max_cap fields appear and save correctly
4. Click Renew on an expired campaign — confirm expires_at advances by 30 days from the old expiry
5. Confirm tone_direction field is gone from all four campaign forms (create, edit, bulk create in CSV upload)
6. In vendorScheduler.js, confirm that if a vendor post was created 2 days ago, processCampaign() returns false (min_gap=3 not met)
</verification>

<success_criteria>
- Migration 046 registered and idempotent (safe to re-run)
- New campaigns default to frequency_cap=3 in all paths (CSV, manual create, PDF sync)
- expires_at auto-set to today+30 when null in all insert paths
- Scheduler blocks new vendor posts if last vendor post from same brand was < min_gap_days ago
- Platform Console brand config shows min_gap_days + platform_max_cap with save
- Renew button adds 30 days from current expires_at (not today)
- tone_direction gone from: CSV_HEADERS, CSV_EXAMPLE, all four console forms, generateVendorCaption() prompt
</success_criteria>

<output>
After completion, create `.planning/quick/260319-q5j-audit-vendor-campaign-posting-frequency-/260319-q5j-SUMMARY.md`
</output>
