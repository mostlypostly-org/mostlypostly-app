---
phase: quick
plan: 260321-v47
type: execute
wave: 1
depends_on: []
files_modified: [src/routes/calendar.js]
autonomous: true
requirements: [QUICK-v47]
must_haves:
  truths:
    - "Week view columns stretch to full viewport height below the control bar"
    - "View toggle and New Post button are in a fixed top row that does not shift when switching views"
    - "Month/week nav arrows sit in a second row below the top controls, right-aligned"
    - "Filter bar uses a single funnel icon that toggles filter chips visible/hidden"
    - "View toggle buttons have more spacing/padding between them"
  artifacts:
    - path: "src/routes/calendar.js"
      provides: "Restructured calendar layout with 2-row controls, funnel filter, full-height week columns"
  key_links:
    - from: "filter funnel icon click"
      to: "filter-bar visibility toggle"
      via: "JS event listener toggling hidden class on #filter-bar"
---

<objective>
Polish the calendar page layout: restructure the top controls into two rows (view toggle + New Post fixed on top, nav arrows below right-aligned), make week view columns full viewport height, replace filter chips with a collapsible funnel icon, and add breathing room to the view toggle buttons.

Purpose: Improve visual hierarchy and usability of the calendar page.
Output: Updated `src/routes/calendar.js` with all layout changes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/calendar.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure top controls, full-height week columns, funnel filter icon</name>
  <files>src/routes/calendar.js</files>
  <action>
Make these changes to the GET `/` route HTML (the `body` template literal starting ~line 250):

**1. Restructure top control bar into two rows (lines 263-320):**

Row 1 (top, sticky): Contains the page title ("Content Calendar"), view toggle (Month/Week/Agenda), and "+ New Post" button. These three items sit in a single flex row. The New Post button is always at the far right. This row should NOT change when switching views.

Row 2 (below row 1): Contains the nav arrows (prev/next month) with the month/year label centered between them. Right-aligned within `max-w-5xl`. This row is hidden in agenda view (existing behavior for `#nav-arrows`). Keep the `id="nav-arrows"` on this row so the existing JS that hides it in agenda view still works.

Remove the gear settings button from the top row -- move it into the filter bar area (or keep it in row 1 next to New Post if simpler -- use judgment, but the key constraint is: view toggle + New Post must not shift).

**2. View toggle spacing:**
Change the view toggle buttons from `px-3 py-1.5 text-xs` to `px-4 py-2 text-sm` for more breathing room. Add `gap-0` to the parent container (it already uses `overflow-hidden` so the rounded corners are fine).

**3. Filter bar -- replace chips with funnel icon toggle (lines 323-337):**

Replace the current always-visible filter chips row with:
- A small funnel/filter icon button (inline SVG funnel pointing down) placed in row 1 or row 2 (use judgment -- near the nav arrows makes sense).
- Clicking the funnel toggles `#filter-bar` visibility (hidden by default on page load).
- The filter chips themselves remain unchanged -- they just start hidden and the funnel reveals/hides them.
- Keep `id="filter-bar"` on the chips container. Add `hidden` class by default.
- In the JS section (~line 572 area where `renderFilterChips()` and `applyFilters()` are called on load), still call `applyFilters()` on load so filters work even when bar is hidden. The chips just aren't visible until the funnel is clicked.

Add JS for funnel toggle:
```js
var funnelBtn = document.getElementById('filter-funnel-btn');
var filterBar = document.getElementById('filter-bar');
if (funnelBtn && filterBar) {
  funnelBtn.addEventListener('click', function(e) {
    e.stopPropagation();
    filterBar.classList.toggle('hidden');
  });
}
```

Funnel SVG icon (use a standard funnel/filter icon):
```html
<button id="filter-funnel-btn" type="button" class="flex h-8 w-8 items-center justify-center rounded-lg border border-mpBorder text-mpMuted hover:bg-mpBg hover:text-mpCharcoal transition-colors" aria-label="Toggle filters">
  <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
    <path stroke-linecap="round" stroke-linejoin="round" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z" />
  </svg>
</button>
```

**4. Week view full-height columns (line 770 area in GET /week):**

Change week day cells from `min-h-[200px]` to `min-h-[calc(100vh-220px)]` so they stretch to fill the viewport minus the top controls (~220px accounts for header + controls + day headers). This makes the week columns tall enough to fill the browser window.

**5. Preserve all existing functionality:**
- SortableJS drag-and-drop must still work
- Day cell click to open day panel must still work
- View switching must still work (switchView function)
- Card settings gear dropdown must still work
- Filter persistence in localStorage must still work
- applyFilters and applyCardSettings must still be called on load and after view switches
  </action>
  <verify>
    <automated>cd /Users/troyhardister/chairlyos/mostlypostly/mostlypostly-app && node -e "import('./src/routes/calendar.js').then(() => console.log('OK')).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <done>
    - View toggle and New Post button are in a stable top row that does not shift between views
    - Nav arrows are in a second row below, right-aligned with month label
    - View toggle buttons have more padding (px-4 py-2 text-sm)
    - Filter chips are hidden by default behind a funnel icon button
    - Clicking the funnel icon shows/hides the filter chips
    - Week view columns extend to near-full viewport height
    - All existing interactivity preserved (drag, filters, day panel, view switching)
  </done>
</task>

</tasks>

<verification>
- Load `/manager/calendar` -- view toggle and New Post button are in top row
- Nav arrows are in a second row below
- Click funnel icon -- filter chips appear; click again -- they hide
- Switch to Week view -- columns fill the viewport height
- Switch between Month/Week/Agenda -- New Post button does not move
- Drag a post between days in month view -- still works
- Click a day cell -- day panel still opens
</verification>

<success_criteria>
All five layout changes applied to calendar.js without breaking existing drag-and-drop, filtering, view switching, or day panel functionality. The page loads and renders without errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260321-v47-calendar-layout-polish-full-height-week-/260321-v47-SUMMARY.md`
</output>
